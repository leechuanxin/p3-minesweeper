<%- include('../headers/html-header', { title: title }); -%><%-
include('../nav/navbar', { user: user, showButton: true, pageState:
"currentGame"}); -%>
<span id="gameId" class="d-none"><%= game.id %></span>
<span id="userId" class="d-none"><%= user ? user.id : 0 %></span>
<div class="container-fluid pt-5 pb-3">
  <div class="row pt-3">
    <div class="col-12 pt-1">
      <div class="row">
        <div class="col-12">
          <p class="mb-0">
            <a href="/"><small>« Back to Home</small></a>
          </p>
        </div>
      </div>
      <hr />
    </div>
    <div class="col-12 pt-1">
      <div class="row">
        <div class="col-8">
          <button
            type="button"
            class="btn btn-info info-button me-3"
            data-bs-toggle="modal"
            data-bs-target="#infoModal"
          >
            <i class="fas fa-question"></i>
          </button>
          <%- include('../modals/rules', { minesToWin }); -%>
        </div>
        <div id="forfeitButtonContainer" class="col-4 text-end"></div>
      </div>
      <hr />
    </div>
    <div class="container-fluid">
      <div class="row">
        <div id="gameOverFeedbackCol" class="col-12"></div>
      </div>
    </div>
    <div class="container-fluid">
      <div class="row m-0">
        <div id="profileWrapper" class="profile-wrapper col-12 col-md-5"></div>
        <div
          id="msWrapper"
          class="ms-wrapper col-12 col-md-7 mt-4 mt-md-0"
        ></div>
      </div>
    </div>
  </div>
</div>
<%- include('../footers/html-footer'); -%>
